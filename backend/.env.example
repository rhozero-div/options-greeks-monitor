# Options Greeks Monitor — Backend Configuration

# CORS allowed origins (comma-separated, or * for all)
CORS_ORIGINS=http://localhost:3000,http://localhost:3011

# Server port
PORT=8000

# Risk-free rate fallback (used when ^IRX fails)
RISK_FREE_RATE=0.05
